/**
 * Mocked data used for user data retrival purposes
 * @type {{user: {firstName: string, lastName: string}}}
 */
const UserService = {
  user: {
    firstName: 'John',
    lastName: 'Kowalski',
  },
};

export default UserService;
